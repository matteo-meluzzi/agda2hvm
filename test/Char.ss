(import (only (chezscheme) record-case))

(define (Char) (begin (display "encountered axiom: Char\n") (exit 1)))

(define (primIsLower) (begin (display "encountered axiom: primIsLower\n") (exit 1)))

(define (primIsDigit) (begin (display "encountered axiom: primIsDigit\n") (exit 1)))

(define (primIsAlpha) (begin (display "encountered axiom: primIsAlpha\n") (exit 1)))

(define (primIsSpace) (begin (display "encountered axiom: primIsSpace\n") (exit 1)))

(define (primIsAscii) (begin (display "encountered axiom: primIsAscii\n") (exit 1)))

(define (primIsLatin1) (begin (display "encountered axiom: primIsLatin1\n") (exit 1)))

(define (primIsPrint) (begin (display "encountered axiom: primIsPrint\n") (exit 1)))

(define (primIsHexDigit) (begin (display "encountered axiom: primIsHexDigit\n") (exit 1)))

(define (primToUpper) (begin (display "encountered axiom: primToUpper\n") (exit 1)))

(define (primToLower) (begin (display "encountered axiom: primToLower\n") (exit 1)))

(define (primCharToNat) (begin (display "encountered axiom: primCharToNat\n") (exit 1)))

(define (primNatToChar) (begin (display "encountered axiom: primNatToChar\n") (exit 1)))

(define 
  (primCharEquality)
  (begin (display "encountered axiom: primCharEquality\n") (exit 1)))